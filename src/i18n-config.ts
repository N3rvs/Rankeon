// Defines the list of supported locales and the default locale.
// This is separated to avoid circular dependencies with navigation components.

export const locales = ['en', 'es', 'fr', 'de', 'it', 'pt'] as const;
export const defaultLocale = 'es';
